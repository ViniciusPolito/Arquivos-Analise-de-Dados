---
title: "Atividade 11"
author: "Vinicius Barbosa Polito Lopes"
date: "25/01/2021"
output: html_document
bibliography: C:\\Users\\vinib\\Documents\\.bib
---

# Questões {.tabset}

## Questão 1

```{r Martin}
library(tm)
library(wordcloud)
library(readr)

martin<-read_file("Martin.txt")
VS <- VectorSource(martin)
corpus<- Corpus(VS)

corpus<- tm_map(corpus, stripWhitespace)
corpus<- tm_map(corpus, removePunctuation)
corpus<- tm_map(corpus, content_transformer(tolower))
corpus<- tm_map(corpus, removeNumbers)
corpus<- tm_map(corpus, removeWords, stopwords("portuguese"))

tdm<- as.matrix(TermDocumentMatrix(corpus))
fre<- sort(rowSums(tdm),decreasing=T)
aux<-subset(fre,fre>2)
#Palavras frequentes#Resposta 1.2
barplot(aux,las=2,col = rainbow(10))

#Nuvem de Palavras#Resposta 1.1
wordcloud(corpus, min.freq = 1, max.words = 30,random.order = F
          ,rot.per = 0.35,colors = brewer.pal(8,"Dark2"))

```

## Questão 2

```{r Black}
library(twitteR)
library(tm)
library(syuzhet)
library(wordcloud)

consumer_key<-"gKKPDkQlrNOvRixhdlZ2FZzYU"
consumer_secret<-"gviohupaUSY16PY43knZ0bj5f1GyFizK3Fr0MU8UyjGHqZCNpi"
access_token<-"1333392592545980416-Z6tuysNufrweztsGxRcdZigCVI6Uph"
access_secret<-"pB5HvIPwIQiaukeK0rh2nqpJOFXwwXi2b2Mw0KgBIdGFQ"

setup_twitter_oauth(consumer_key,consumer_secret,access_token,access_secret)

tweets<- searchTwitter("#BlackLivesMatter",n=500,lang="en")
tweets<- twListToDF(tweets)
tweets_t<- paste(tweets$text,collapse = " ")
tweets_S<- VectorSource(tweets_t)
corpus<-Corpus(tweets_S)

corpus<- tm_map(corpus, tolower)
corpus<- tm_map(corpus, removePunctuation)
corpus<- tm_map(corpus, stripWhitespace)
corpus<- tm_map(corpus, removeWords, stopwords("english"))

removeURL<-function(x) gsub("http[^[:space:]]*", "",x)
corpus<- tm_map(corpus,removeURL)

removeNumPunct<- function(x) gsub("[^[:alpha:][:space:]]*","",x)
corpus<- tm_map(corpus,content_transformer(removeNumPunct))

dtm<-TermDocumentMatrix(corpus)
dtm<- as.matrix(dtm)

fre<- sort(rowSums(dtm),decreasing=T)
#Resposta 2.1
wordcloud(corpus,min.freq = 2,max.words = 30,random.order = F,rot.per = 0.15,colors = brewer.pal(8,"Dark2"),scale = c(8,.2))

#Resposta 2.2
s<-get_nrc_sentiment(tweets_t)
barplot(colSums(s), las=2,col = rainbow(10),
        ylab = "Contagem",main = "Sentimentos com Relação ao STF")



```

## Questão 3

$$ f(x) = \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3 + \beta_4 (x-\xi)^3_+ $$
$$
(x-\xi)^3_+ =
\begin{cases}
 (x-\xi)^3\, ,& x>\xi \\
 0  ,& x\leq\xi
 \end{cases}       
$$
$$
\begin{aligned}
  (x \leq \xi) \Rightarrow f(x) &= \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3 \\
  (x > \xi) \Rightarrow f(x) &= \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3 + \beta_4(x^3 -3x^2\xi + 3x\xi^2 - \xi^3) \\
  &=(\beta_0 + \beta_4\xi^3) + (\beta_1 + 3\beta_4\xi^2)x + (\beta_2 - 3\beta_4)
\end{aligned}
$$
$$ \\frac{\\partial \\bar{x}}{\\partial t} $$
$$ \\alpha_{\\beta}^{\\ga,,a} $$

## Questão 4

![Foto 1](C:/Users/vinib/Downloads/download.jpg) 
![Foto 2](C:/Users/vinib/Downloads/downloade.jpg)


```{r Tabela R}
library(DT)
datatable(airquality)
datatable(ToothGrowth)
```

## Questão 5

@gable2000search

@croft2010search

@sampaio2018reducing

@sampaio2019decentralized

@sampaio2019multi




### Referências
